```json
{
  "summary": "Добавлен POST /api/reviews, который принимает diff и передаёт его в ReviewService.review(). В ReviewService добавлен метод review: формирует prompt с сырым diff, вызывает внешний LLM через llm.generate() и возвращает {\"comment\": answer}.",
  "risks": [
    {
      "file": "app/review_service.py",
      "line": 15,
      "evidence": "На строке 14 формируется prompt с сырым diff, а на строке 15 этот prompt передаётся внешнему LLM через llm.generate(). В показанном коде не видно удаления секретов, требуемого SEC-1.",
      "risk": "Если очистка не выполняется до вызова review(), секреты из diff могут быть утечены во внешний LLM."
    },
    {
      "file": "app/review_service.py",
      "line": 16,
      "evidence": "Метод review() возвращает {\"comment\": answer}. По OUT-1 ответ должен содержать summary, массив risks и массив checks.",
      "risk": "Клиенты не получат требуемую структуру ответа; интеграции сломаются или потребуют переделки."
    },
    {
      "file": "app/api.py",
      "line": 9,
      "evidence": "Хендлер create_review принимает payload и сразу пересылает diff в сервис. Проверки размера входа на 20 000 символов (API-1) не показано.",
      "risk": "Слишком длинный diff не будет отклонён с HTTP 413; возможны превышения лимитов LLM и деградация производительности."
    }
  ],
  "checks": [
    "SEC-1; Передать в ReviewService.review() diff с включённым значением token=test-secret; перехватить аргумент llm.generate(); ожидаем, что секрет заменён на [REDACTED] перед отправкой.",
    "OUT-1; POST /api/reviews с телом {\"diff\": \"x\"}; ожидаем JSON со свойствами summary, risks[], checks[]; текущая реализация вернёт только {comment: ...}.",
    "API-1; POST /api/reviews с diff длиной 20001 символ; ожидаем HTTP 413 и контролируемое тело ошибки; текущая реализация вызовет сервис без отклонения."
  ]
}
```
