Результат ревью по формату OUT-1:

```json
{
  "summary": "Добавлен endpoint POST /api/reviews, который передаёт payload[\"diff\"] в ReviewService.review. Сервис формирует prompt с включённым diff, вызывает llm.generate и возвращает {\"comment\": answer}. В представленном фрагменте отсутствуют локальные меры по SEC-1 (очистка секретов до вызова LLM), не видно проверки ограничения длины (API-1) и таймаут/обработка ошибок внешнего вызова (REL-1).",
  "risks": [
    {
      "file": "app/review_service.py",
      "line": 19,
      "evidence": "def review(self, diff: str) -> dict[str, str]:\n        prompt = f\"Review this pull request and find problems:\n{diff}\"\n        answer = self.llm.generate(prompt)",
      "risk": "SEC-1: diff включается в prompt без локальной очистки секретов до внешнего LLM-вызова. Очистка в клиенте LLM неизвестна; при её отсутствии возможна утечка секретов."
    },
    {
      "file": "app/api.py",
      "line": 8,
      "evidence": "@app.post(\"/api/reviews\")\ndef create_review(payload: dict) -> dict[str, str]:\n    return review_service.review(payload[\"diff\"])",
      "risk": "API-1: отсутствует проверка длины diff и возврат 413 при превышении 20 000 символов в данном обработчике. Если нет внешних ограничений, большие диффы пройдут к LLM."
    },
    {
      "file": "app/review_service.py",
      "line": 21,
      "evidence": "answer = self.llm.generate(prompt)\nreturn {\"comment\": answer}",
      "risk": "REL-1: не видно таймаута 10 секунд и контролируемого ответа на ошибку внешнего вызова. Если generate выбросит исключение/таймаут, возможен 500 без дружелюбного ответа."
    }
  ],
  "checks": [
    {
      "input": "diff содержит маркер \"SECRET_TOKEN=abcd...\"",
      "action": "Отправить POST /api/reviews с телом {diff: '...SECRET_TOKEN=abcd...'}; перехватить исходящий запрос к внешнему LLM (или заглушке)",
      "expected": "В исходящем запросе нет исходного токена (SEC-1). При отсутствии очистки зафиксировать дефект."
    },
    {
      "input": "diff длиной 20001 символ",
      "action": "POST /api/reviews с валидным JSON; измерить ответ",
      "expected": "HTTP 413 и сообщение о превышении лимита (API-1)."
    },
    {
      "input": "короткий diff; внешняя заглушка LLM отвечает с задержкой >10 c или бросает ошибку",
      "action": "POST /api/reviews; заглушка LLM задерживает ответ на 11 c или генерирует исключение",
      "expected": "Контролируемый ответ (например, 502/503) без трассировки; время ожидания не превышает 10 c (REL-1)."
    }
  ]
}
```

Ограничения анализа:

- Рассмотрен только `TRAINING_PR.diff`. Реализация клиента LLM, глобальные middleware/валидации и ограничения шлюза неизвестны. Отсутствие проверки/очистки в локальном коде не доказывает их отсутствие в других слоях. Выводы о них отмечены как риск и требуют проверки.
