# Integration-проверки

| Связь компонентов | Вход и воспроизведение | Наблюдаемая проверка | Ожидаемый результат | Ожидаемое evidence | Статус |
|---|---|---|---|---|---|
| API -> ReviewService -> LLM | POST /api/reviews с diff ровно 20 000 символов; внешний LLM заменён заглушкой, возвращающей валидный ответ по OUT-1 | API-1 на границе: запрос не отклоняется; LLM вызывается один раз; структура ответа воспроизводимо проверяется | Запрос принят; ответ соответствует OUT-1 (summary, checks, ≤3 risks с file, line, evidence, risk) | Трасс/метрика: count вызовов LLM=1; тело ответа соответствует OUT-1 | не выполнялся |
| API -> ReviewService -> (нет вызова LLM) | POST /api/reviews с diff длиной 20 001 символ | API-1: превышение размера приводит к отказу; отсутствие побочных вызовов | HTTP 413; ReviewService и внешний LLM не вызываются | Ответ API со статусом 413; метрика/spy: вызовы ReviewService=0, LLM=0 | не выполнялся |
| ReviewService -> LLM | Передать в ReviewService.review() diff, содержащий token=test-secret, password=p@ss, приватный ключ (начало -----BEGIN PRIVATE KEY-----) | SEC-1: перед внешним вызовом секреты удалены; остальная часть diff сохранена | В llm.generate() секреты отсутствуют; остальной текст diff сохранён без потерь | Перехваченный аргумент llm.generate(): отсутствуют исходные секреты; совпадение остальных фрагментов diff | не выполнялся |
| API -> ReviewService -> Validator ответа | Использовать заглушку внешнего LLM, возвращающего JSON с 4 risks: первый — неподтверждённый (нет строки diff/правила), далее три подтверждённых | OUT-1 и QA-1: структура ответа, ≤3 risks; неподтверждённый риск удалён, подтверждённые сохранены | В ответе остаются три подтверждённых риска; каждый имеет file, line, evidence, risk; неподтверждённого риска нет | Ответ сервиса после нормализации; сопоставление evidence с diff/правилом подтверждает оставшиеся три риска | не выполнялся |
| API -> Логирование | Выполнить успешный запрос с произвольным diff | OBS-1: в логах только request_id, длительность, статус; отсутствие diff, секретов и ответа LLM | Логи содержат только разрешённые поля; чувствительные/лишние данные отсутствуют | Снимок лог-записи: request_id, duration_ms, status; отсутствие полей diff/llm_response/secret | не выполнялся |
| ReviewService -> LLM (timeout) | Заглушка LLM задерживает generate() >10с | REL-1: ограничение внешнего вызова 10с; измеряется длительность именно вызова LLM, а не всего HTTP; контролируемый ответ требуется, но точные HTTP-статус и тело не определены Context Pack | Возвращается контролируемый ответ; длительность внешнего вызова ≈10с (допуск измерения как предложение тестового стенда) | Измерение t_end - t_start вокруг llm.generate(): ~10.0s±0.2s (допуск из-за разрешения таймера и планировщика ОС); логи содержат статус и длительность | не выполнялся |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-02
- Что проверили и исправили сами: проверяемость через API; наличие evidence полей. Проверка человеком ожидается.
