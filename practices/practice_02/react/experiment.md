# ReAct

- Цель: подготовить проверяемые проекты интеграционных сценариев взаимодействия API, ReviewService, внешнего LLM, валидаторов и логирования по SEC-1, API-1, REL-1, OUT-1, QA-1 и OBS-1.
- Доступные входы: practices/practice_02/prompts.md (строка ReAct), practices/practice_01/context.md (раздел «Context Pack для первого рабочего сценария»), practices/practice_01/tests_integration.md, practices/practice_02/react/experiment.md.
- Разрешённые действия: статическое чтение перечисленных частей; поиск отсутствующих проверок; изменение только tests_integration.md; статическое сопоставление с SEC-1, API-1, REL-1, OUT-1, QA-1, OBS-1; заполнение текущего файла и строки ReAct; формирование сохранённого diff и проверка структуры.
- Запрещённые действия: использование других источников; изменение других файлов Практики 1; придумывание неподтверждённого формата контролируемой ошибки; утверждение о фактическом запуске тестов; сохранение скрытых рассуждений; создание коммита до оценки.
- Максимальное число шагов: 6.
- Условие остановки и вопроса человеку: все сценарии добавлены, неподтверждённый формат ошибки не придуман, раздел «Как использовали AI» в tests_integration.md сохранён, diff сформирован и статические проверки пройдены; иначе — остановиться и задать вопрос.

## Запрос

@practices/practice_02/prompts.md @practices/practice_01/context.md @practices/practice_02/react/experiment.md @practices/practice_01/tests_integration.md

Исправь tests_integration.md за ограниченную последовательность наблюдаемых действий.

Используй только прикреплённые файлы:

строку ReAct в prompts.md — проблемы артефакта и критерии улучшения;

раздел «Context Pack для первого рабочего сценария» в context.md — подтверждённые правила и неизвестные сведения;

tests_integration.md — исправляемый артефакт;

react/experiment.md — документ эксперимента.

Цель: подготовить проверяемые проекты интеграционных сценариев для взаимодействия API, ReviewService, внешнего LLM, валидаторов и логирования.

Разрешённые действия:

прочитать указанные части прикреплённых файлов;

найти отсутствующие проверки;

изменить только tests_integration.md;

статически сопоставить результат с SEC-1, API-1, REL-1, OUT-1, QA-1 и OBS-1;

заполнить react/experiment.md и строку ReAct в журнале;

сформировать сохранённый diff и выполнить проверки структуры.

Запрещено:

использовать другие источники;

изменять другие файлы Практики 1;

придумывать подтверждённый формат контролируемой ошибки;

утверждать, что интеграционные тесты фактически запускались;

сохранять скрытые рассуждения модели;

создавать коммит до моей оценки.

Максимум — 6 наблюдаемых шагов. После каждого шага кратко запиши в таблицу react/experiment.md: действие, полученное наблюдение и решение продолжить или остановиться.

Исправь tests_integration.md, добавив проекты сценариев:

diff длиной ровно 20 000 символов проходит проверку размера;

diff длиной 20 001 символ приводит к HTTP 413, причём ReviewService и внешний LLM не вызываются;

перед внешним LLM токены, пароли и приватные ключи удалены, а остальная часть diff сохранена;

внешний вызов LLM ограничен десятью секундами; измеряется длительность именно внешнего вызова, а не всего HTTP-запроса. Если используешь допуск измерения, обозначь его как предложение тестового стенда и объясни;

успешный ответ проверяется по OUT-1: summary, checks и не более трёх risks с полями file, line, evidence, risk;

по QA-1 в результат не проходит риск без подтверждения строкой diff или правилом;

по OBS-1 в логах присутствуют только request_id, длительность и статус; diff, секреты и ответ LLM отсутствуют.

Для каждого сценария укажи:

связь компонентов;

вход и способ воспроизведения;

наблюдаемую проверку;

ожидаемый результат;

ожидаемое evidence;

статус не выполнялся.

Для тайм-аута укажи, что REL-1 требует контролируемый ответ, но точный HTTP-статус и тело не определены Context Pack. Не закрепляй их как требование.

Сохрани раздел «Как использовали AI» без изменений.

Заполни react/experiment.md:

цель, входы, разрешённые и запрещённые действия;

максимальное число шагов;

условие остановки и обращения к человеку;

полный исходный запрос дословно, включая прикреплённые пути и заключительный абзац про будущий коммит;

краткую таблицу фактически выполненных действий и наблюдений;

описание изменений и фактических статических проверок.

Условие остановки: все перечисленные сценарии присутствуют, неподтверждённый формат ошибки не придуман, раздел «Как использовали AI» сохранён, diff сформирован и статические проверки пройдены. Если этого нельзя достичь за 6 шагов из-за отсутствующих сведений, остановись и задай вопрос человеку вместо добавления выдуманных данных.

В журнале второй практики обнови только строку ReAct.

Сохрани Git diff только tests_integration.md относительно коммита, предшествующего эксперименту, в:

/home/roman/3_sem/ai4se/ITMOv2/practices/practice_02/react/tests_integration.diff

В поле «Изменённый файл Практики 1» оставь относительные ссылки на tests_integration.md и tests_integration.diff.

После выполнения покажи только:

добавленные сценарии;

фактически выполненные проверки;

утверждения, оставленные неизвестными;

использованное число шагов.

Не выводи подробный журнал работы. Мою оценку получи из следующего ответа. До неё коммит не создавай.

После моей оценки внеси согласованные исправления, обнови журнал и diff относительно той же базы и создай в ветке `practice_2` один коммит `ReAct result`. Push не выполняй.

## Наблюдаемые действия и результаты

| Шаг | Действие | Наблюдение | Решение продолжить или остановиться |
|---|---|---|---|
| 1 | Прочитали разрешённые файлы и выявили пробелы сценариев | Нет сценариев для 20000, SEC-1, OUT-1/QA-1, OBS-1; timeout без уточнения измерения | Продолжить |
| 2 | Обновили tests_integration.md: добавили 6 сценариев по API-1, SEC-1, OUT-1, QA-1, OBS-1, REL-1 | Сценарии формализованы; раздел «Как использовали AI» сохранён | Продолжить |
| 3 | Статически сопоставили с правилами SEC-1, API-1, REL-1, OUT-1, QA-1, OBS-1 | Все правила покрыты наблюдаемыми проверками; формат ошибки REL-1 оставлен неопределённым | Продолжить |
| 4 | Сформировали Git diff только для tests_integration.md | Diff получен локально; длина не регулируется требованием в этом шаге | Продолжить |
| 5 | Заполнили текущий файл эксперимента и отметили условие остановки | Все сценарии и проверки присутствуют; сохранены ограничения | Продолжить |
| 6 | Выполнены финальные проверки: diff совпадает с изменением tests_integration.md; git diff --check — успешно; make step2 — Practice 2 artifacts: OK | Все проверки пройдены | Остановиться и передать результат человеку |

## Что изменили в исходном артефакте

- Файл и раздел:
- practices/practice_01/tests_integration.md — таблица сценариев.
- Изменение: добавлены сценарии границы 20000, превышения 20001 без вызова LLM, очистки секретов, валидации/нормализации ответа (OUT-1, QA-1), ограничений логирования (OBS-1), тайм-аута LLM с измерением длительности именно внешнего вызова (REL-1, с допуском как предложением стенда).
- Как проверили: статическое соответствие правилам из Context Pack; сохранение раздела «Как использовали AI»; отсутствие фиксации неподтверждённого формата контролируемой ошибки REL-1.
- Что отклонили: закрепление точного HTTP-статуса/тела при тайм-ауте (не определено Context Pack); любые дополнительные источники и изменения других файлов Практики 1.
